config.env
*.env
!config.example.env
__pycache__/
*.pyc
.pytest_cache/
*.zip
.venv/
venv/

# Generated by `agentcore configure`/`agentcore deploy` - account-specific,
# regenerated fresh by each person who runs those commands themselves.
.bedrock_agentcore.yaml
.bedrock_agentcore/
.dockerignore

# Internal planning doc + client deliverables - handed over separately
# (DOCX/video), not part of the source code repo itself.
CLAUDE.md
docs/AgentCore_Project_Guide.docx
docs/STEPS.md
docs/generate_docx.py

# Outputs written by the cloud test scripts
cloud_tests/.last_run.json
cloud_tests/.speed_comparison.json

# Generated by setup/06_configure_runtime.py from config.env. Contains the
# LLM API key as a runtime environment variable, so it is never committed -
# same reason config.env isn't. Regenerate it with that script.
agentcore/agentcore.json
agentcore/.env.local
agentcore/cdk/cdk.out/
agentcore/cdk/node_modules/
agentcore/.cli/logs/
